import SwiftUI

struct BreatheColors {
    static let primary = Color("PrimaryColor") // Açık nane yeşili
    static let secondary = Color("SecondaryColor") // Soft lavanta
    static let background = Color("BackgroundColor") // Çok açık bej
    static let accent = Color("AccentColor") // Yumuşak turkuaz
    static let text = Color("TextColor") // Koyu gri
    
    // Faz renkleri
    static let inhale = Color("InhaleColor") // Yumuşak mavi
    static let hold = Color("HoldColor") // Sakin mor
    static let exhale = Color("ExhaleColor") // Pastel yeşil
} 